<div class="back_home">
  <%= link_to "Back To Home Page", root_path %>
</div>

<center><div class="header_sign_up">
<h2>Edit <%= resource_name.to_s.humanize %> Profile</h2>
</div></center>

<center><div id="sign_up_box">
<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <center>
  <%= devise_error_messages! %>
  </center>
  <center>
  <div class="field">
    <%= f.text_field :name, placeholder: "EDIT NAME HERE", autocomplete: "off" %>
  </div>

  <div class="field">
    <%= f.email_field :email, placeholder: "EDIT EMAIL HERE", autocomplete: "off", autofocus: true %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="field">
    <i><font color="white">(Leave blank if you don't want to change it)</font></i><br />
    <%= f.password_field :password, placeholder: "ENTER NEW PASSWORD HERE", autocomplete: "off" %>
  </div>

  <div class="field">
    <%= f.password_field :password_confirmation, placeholder:"CONFIRM NEW PASSWORD HERE", autocomplete: "off" %>
  </div>

  <div class="field">
    <i><font color="white">(We need your current password to confirm your changes)</font></i><br />
    <%= f.password_field :current_password, placeholder:"ENTER OLD PASSWORD HERE", autocomplete: "off" %>
  </div>

  <div class="actions">
    <%= f.submit "Update" %>
  </div>
  </center>
<% end %>

<h3>Not Happy With US? Delete This Account Below:</h3>
<p><%= button_to "Cancel my account", registration_path(resource_name), data: { confirm:"Are you sure that you truly wish to leave us? We cannot imagine why...We at Droughts Are Us work very hard to bring you data that is both accurate and relevant. If you believe that there is something we can do better, we don't really care! If you do not want to be informed about what is going on in your country and what the trends have been in order to better predict the future states of the areas you live near, then you are not even worth the trouble of us begging you to stay. So good luck out there. Stay cool and stay hydrated. Otherwise, if you change your mind, please click the cancel button." }, method: :delete %></p>
</div>
