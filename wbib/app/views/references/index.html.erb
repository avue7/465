<center><h1>Athit's Webliography Program</h1>

<style>
table, th, td {
    border: 1px solid black;
    border-collapse: collapse;
}
th, td {
    padding: 5px;
}
</style>
<table align="center"style="width:80%">
  <thead>
    <tr>
      <th>Topics:</th>
      <th colspan="3"> Options: </th>
    </tr>
  </thead>

  <tbody>
    <% @references.each do |reference| %>
      <tr>
        <td><%= reference.topic %></td>
        <td><%= link_to 'Show', reference %></td>
        <td><%= link_to 'Edit', edit_reference_path(reference) %></td>
        <td><%= link_to 'Delete', reference, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<form action=new_reference_path>
    <input type="submit" value="New Reference">
</form>
<%= link_to 'New Reference', new_reference_path %></center>
<button type="button" onclick=link_to 'New Reference', new_reference_path>
Reference</button>

